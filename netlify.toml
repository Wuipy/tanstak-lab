[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_URL = "https://lab5progra4.runasp.net"

# SPA: rutas de React Router / TanStack Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
